# README

## Setup Database

- $ rails db:create
- $ rails db:migrate
- $ rails db:seed
- $ gem install rack-cors //Enable CORS in Rails
- in gemfile add gem 'rack-cors', :require => 'rack/cors'
- $ bundle install
